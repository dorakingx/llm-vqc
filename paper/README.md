# Paper source

`main.tex` and `references.bib` are the unified manuscript draft for the LLM-VQC/QAE study.

The requested local/Overleaf path `Developer/Overleaf/llm-vqc` is not exposed by the connected tools in this ChatGPT session. This repository directory is therefore the canonical source produced here.

To sync it to the existing Overleaf project, copy:
- `paper/main.tex`
- `paper/references.bib`
- the QAE figures from `outputs/qae_tfim_semantic_pilot_v1/`

The manuscript deliberately labels the QAE result as an exploratory semantic-prior pilot and does not claim general LLM superiority or quantum advantage.
